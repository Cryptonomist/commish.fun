warning: in the working copy of 'programs/commish/src/lib.rs', LF will be replaced by CRLF the next time Git touches it
[1mdiff --git a/programs/commish/src/lib.rs b/programs/commish/src/lib.rs[m
[1mindex d2a7566..ca2f86c 100644[m
[1m--- a/programs/commish/src/lib.rs[m
[1m+++ b/programs/commish/src/lib.rs[m
[36m@@ -186,8 +186,24 @@[m [mpub mod commish {[m
             // Strictly increasing, and the first week this pool plays must not[m
             // have kicked off yet. A pool created after its own lock would take[m
             // buy-ins for a week nobody can pick.[m
[32m+[m[32m            /* Strictly increasing is not enough: a week has to leave room for[m
[32m+[m[32m             * its own result cycle. `post_results` cannot run until[m
[32m+[m[32m             * MIN_POST_DELAY_SECS after the lock, and `finalize_week` waits a[m
[32m+[m[32m             * further `dispute_window_secs` after that. If the next lock lands[m
[32m+[m[32m             * before finalization can happen, `advance_week` never runs, week[m
[32m+[m[32m             * N+1 opens already locked, every member misses a pick, and the[m
[32m+[m[32m             * pool is unplayable — after the buy-ins are collected.[m
[32m+[m[32m             *[m
[32m+[m[32m             * With the 7-day maximum dispute window this needs a gap longer[m
[32m+[m[32m             * than 171 hours, which is longer than a real NFL week. That is a[m
[32m+[m[32m             * true constraint, not an oversight: a pool wanting week-long[m
[32m+[m[32m             * disputes has to run a shorter schedule or a smaller window. */[m
[32m+[m[32m            let min_gap = rules::min_week_gap(dispute_window_secs)?;[m
             for w in 1..WEEKS {[m
[31m-                require!(lock_ts[w] > lock_ts[w - 1], CommishError::BadSchedule);[m
[32m+[m[32m                let gap = lock_ts[w][m
[32m+[m[32m                    .checked_sub(lock_ts[w - 1])[m
[32m+[m[32m                    .ok_or(CommishError::MathOverflow)?;[m
[32m+[m[32m                require!(gap > min_gap, CommishError::BadSchedule);[m
             }[m
             require!([m
                 lock_ts[start_week as usize - 1] > now,[m
[36m@@ -601,6 +617,8 @@[m [mpub mod commish {[m
     /// Close the week: either the pool is decided, or it moves on.[m
     pub fn advance_week(ctx: Context<AdvanceWeek>) -> Result<()> {[m
         let vault_amount = ctx.accounts.vault.amount;[m
[32m+[m[32m        let (commissioner, nonce, bump) = ctx.accounts.pool.seed_parts();[m
[32m+[m[32m        let mut fee_to_pay: u64 = 0;[m
         let pool = &mut ctx.accounts.pool;[m
 [m
         require_eq!(pool.status, STATUS_FINALIZED, CommishError::NotFinalized);[m
[36m@@ -630,18 +648,14 @@[m [mpub mod commish {[m
             require!(winners_count > 0, CommishError::NotSettled);[m
             // The fee is computed once, here, off the vault as it stands, and[m
             // capped. Season 1 has fee_bps == 0, so this is zero.[m
[31m-            let fee = if pool.fee_bps == 0 {[m
[31m-                0u64[m
[31m-            } else {[m
[31m-                let raw = (vault_amount as u128)[m
[31m-                    .checked_mul(pool.fee_bps as u128)[m
[31m-                    .ok_or(CommishError::MathOverflow)?[m
[31m-                    / BPS_DENOM as u128;[m
[31m-                (raw as u64).min(pool.fee_cap)[m
[31m-            };[m
[32m+[m[32m            let fee = rules::platform_fee(vault_amount, pool.fee_bps, pool.fee_cap)?;[m
             let payable = vault_amount[m
                 .checked_sub(fee)[m
                 .ok_or(CommishError::MathOverflow)?;[m
[32m+[m[32m            if fee > 0 && !pool.fee_paid {[m
[32m+[m[32m                fee_to_pay = fee;[m
[32m+[m[32m            }[m
[32m+[m[32m            pool.fee_paid = true;[m
             // Integer division. The dust stays in the vault rather than being[m
             // handed to whoever claims first.[m
             pool.pot_per_winner = payable / winners_count as u64;[m
[36m@@ -666,6 +680,35 @@[m [mpub mod commish {[m
                 alive_count: pool.alive_count,[m
             });[m
         }[m
[32m+[m
[32m+[m[32m        /* The fee moves here, and this is the fix to a real defect: every[m
[32m+[m[32m         * earlier version deducted it from the winners' pot and transferred it[m
[32m+[m[32m         * nowhere, leaving it as permanently unclaimable vault residue. It was[m
[32m+[m[32m         * invisible because season 1 runs at fee_bps == 0.[m
[32m+[m[32m         *[m
[32m+[m[32m         * `Pool::fee_paid` was declared from the first commit and never written[m
[32m+[m[32m         * by anything. A field that only ever holds its default is usually the[m
[32m+[m[32m         * fingerprint of an unwritten branch; this is what it was waiting for.[m
[32m+[m[32m         *[m
[32m+[m[32m         * Note this is the fee only. The division remainder in `pot_per_winner`[m
[32m+[m[32m         * still stays in the vault, deliberately — that is documented, bounded[m
[32m+[m[32m         * by `winners_count` micro-USDC, and not the same thing. */[m
[32m+[m[32m        if fee_to_pay > 0 {[m
[32m+[m[32m            let seeds: &[&[u8]] = &[SEED_POOL, commissioner.as_ref(), &nonce, &[bump]];[m
[32m+[m[32m            let signer: &[&[&[u8]]] = &[seeds];[m
[32m+[m[32m            transfer([m
[32m+[m[32m                CpiContext::new_with_signer([m
[32m+[m[32m                    ctx.accounts.token_program.key(),[m
[32m+[m[32m                    Transfer {[m
[32m+[m[32m                        from: ctx.accounts.vault.to_account_info(),[m
[32m+[m[32m                        to: ctx.accounts.fee_treasury_ata.to_account_info(),[m
[32m+[m[32m                        authority: ctx.accounts.pool.to_account_info(),[m
[32m+[m[32m                    },[m
[32m+[m[32m                    signer,[m
[32m+[m[32m                ),[m
[32m+[m[32m                fee_to_pay,[m
[32m+[m[32m            )?;[m
[32m+[m[32m        }[m
         Ok(())[m
     }[m
 [m
[36m@@ -741,6 +784,28 @@[m [mpub mod commish {[m
             require!(ctx.accounts.member.paid, CommishError::MemberNotPaid);[m
             require!(!ctx.accounts.member.claimed, CommishError::AlreadyClaimed);[m
             require!(pool.paid_members > 0, CommishError::NotAMember);[m
[32m+[m
[32m+[m[32m            /* A finalized payout sheet leaves a league at STATUS_SHEET_FINALIZED,[m
[32m+[m[32m             * not STATUS_SETTLED — so the `!= STATUS_SETTLED` guard above lets[m
[32m+[m[32m             * this instruction run while prizes are still owed. Without this[m
[32m+[m[32m             * check the first member past the refund deadline splits the whole[m
[32m+[m[32m             * vault pro rata, and the assignee's `claim_prize` then pays[m
[32m+[m[32m             * `amount.min(vault.amount)` == 0 while still burning their slot.[m
[32m+[m[32m             *[m
[32m+[m[32m             * The deadman still has to work, or an assignee who never claims[m
[32m+[m[32m             * strands the vault forever. So the block expires: PRIZE_CLAIM_GRACE[m
[32m+[m[32m             * after the refund deadline the refund reopens and unclaimed prizes[m
[32m+[m[32m             * fall back into the pro-rata split. */[m
[32m+[m[32m            let outstanding = pool.prize_slots[..pool.slot_count as usize][m
[32m+[m[32m                .iter()[m
[32m+[m[32m                .any(|s| s.state == SLOT_PENDING || s.state == SLOT_FINALIZED);[m
[32m+[m[32m            if outstanding {[m
[32m+[m[32m                let grace_ends = pool[m
[32m+[m[32m                    .refund_deadline_ts[m
[32m+[m[32m                    .checked_add(PRIZE_CLAIM_GRACE_SECS)[m
[32m+[m[32m                    .ok_or(CommishError::MathOverflow)?;[m
[32m+[m[32m                require!(now >= grace_ends, CommishError::RefundNotAvailable);[m
[32m+[m[32m            }[m
         }[m
 [m
         let pool = &mut ctx.accounts.pool;[m
[36m@@ -1240,8 +1305,23 @@[m [mpub struct SettleMember<'info> {[m
 pub struct AdvanceWeek<'info> {[m
     #[account(mut)][m
     pub pool: Box<Account<'info, Pool>>,[m
[31m-    #[account(address = pool.vault @ CommishError::BadVault)][m
[32m+[m[32m    #[account(mut, address = pool.vault @ CommishError::BadVault)][m
     pub vault: Account<'info, TokenAccount>,[m
[32m+[m[32m    /* The fee has a destination now. It is required on every call, including[m
[32m+[m[32m     * the ones that only roll the week forward and the ones where fee_bps is[m
[32m+[m[32m     * zero, because an Anchor account list is static — an account that is[m
[32m+[m[32m     * sometimes absent is an account that is sometimes unchecked.[m
[32m+[m[32m     *[m
[32m+[m[32m     * OPERATIONAL: this token account must exist before fees are switched on.[m
[32m+[m[32m     * With fee_bps == 0 nothing is transferred but the account is still[m
[32m+[m[32m     * deserialized, so a missing treasury ATA stops pools from settling. */[m
[32m+[m[32m    #[account([m
[32m+[m[32m        mut,[m
[32m+[m[32m        constraint = fee_treasury_ata.owner == pool.fee_treasury @ CommishError::BadFeeTreasury,[m
[32m+[m[32m        constraint = fee_treasury_ata.mint == pool.usdc_mint @ CommishError::WrongMint[m
[32m+[m[32m    )][m
[32m+[m[32m    pub fee_treasury_ata: Account<'info, TokenAccount>,[m
[32m+[m[32m    pub token_program: Program<'info, Token>,[m
 }[m
 [m
 #[derive(Accounts)][m
